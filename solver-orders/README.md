# Solver Orders

A caching layer over the orderbook that serves unfulfilled intents to solvers for settlement.

## Overview

The Solver Orders service acts as an intermediary layer between the orderbook and solvers. It caches unfulfilled intents and provides them to solvers for efficient settlement.
